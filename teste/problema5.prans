begin
procedure mdc(int n, m, r) {
    //inicio:
        if (n == 0) {
            r = m;
            // goto fim;
        } elseif (m == 0) {
            r = n;
            // goto fim;
        } elseif (n == 1 or m == 1) {
            r = 1;
            // goto fim;
        } elseif (n > m) {
            n = n % m;
            //goto inicio;
        } else {
            m = m % n;
            //goto inicio;
        }

    //fim:
}

main{
    int n = 0, m = 0;
    print("Digite o valor de n: ");
    n = input();
    print("Digite o valor de m: ");
    m = input();

    int resultado =0;
    mdc(n, m, resultado);

    print("O maior divisor comum entre %d e %d é: %d\n", n, m, resultado);
}
end